import { Box, Button, Text, Spinner } from "@chakra-ui/react";
import CoverLetter from "./CoverLetter";

const Home = () => {
  return (
    <Box>
      <CoverLetter />
    </Box>
  );
};

export default Home;
